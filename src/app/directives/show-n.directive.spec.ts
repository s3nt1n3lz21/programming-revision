import { ShowNDirective } from './show-n.directive';

describe('ShowNDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowNDirective();
    expect(directive).toBeTruthy();
  });
});
